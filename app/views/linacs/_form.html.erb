<%= form_for(@linac) do |f| %>
  <% if @linac.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@linac.errors.count, "error") %> prohibited this linac from being saved:</h2>

      <ul>
      <% @linac.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :user_group_id %><br>
    <%= f.collection_select :user_group_id, UserGroup.all.order(:created_at),:id,:name, include_blank: false %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
